version: '3'

services:
    rutorrent:
        build:
            context: .
            dockerfile: ./Dockerfile
        image: nemd/rutorrent
        stdin_open: false
        tty: false
        environment:
          - TERM=xterm
        ports:
            - 8080:80
            - 49264:49264
        volumes:
            - ~/Downloads:/downloads